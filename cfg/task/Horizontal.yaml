# used to create the object

defaults:
  - Crossing
  - _self_


name: Crossing
asset: 'assets/config/terrain_mutil_batten.yaml'

task:

  reward_coef:
    pitch_angle: 0.25
    roll_angle: 0.25


  actionMode: continuous_std_4

env:

  max_v: 0.2
  max_w: 0.2
  flipper_dt: 4



